# PoC-C：原生置頂貼圖測試

## 目標

將 PoC-B 輸出的 PNG 建立為獨立 Windows 置頂貼圖，並驗證多貼圖、移動、縮放與透明度互動不會阻塞 CaptureFlow 主程式。

## 操作方式

1. 執行框選並輸出圖片。
2. 在結果面板按「建立置頂貼圖」。
3. 在貼圖任意位置按住滑鼠左鍵拖曳以移動。
4. 使用滑鼠滾輪縮放；向上放大、向下縮小。
5. 按住 `Ctrl` 使用滾輪調整透明度。
6. 按 `Esc` 或在貼圖上按滑鼠右鍵關閉。
7. 重複框選與建立貼圖，測試同時開啟多張貼圖。

## PoC-C.2 貼圖工具列

- 工具列建立後預設持續顯示，並使用獨立置頂視窗，不改變貼圖的原始尺寸與座標。
- 貼圖上方有 38 px 空間時放在上方；上方不足但下方有空間時移到下方。
- 上下方都不足時才覆蓋在圖片頂端，適用於全螢幕或接近全螢幕的貼圖。
- 工具列寬度至少 360 px；超出虛擬桌面時會限制並校正水平位置。
- 貼圖拖曳或縮放後，工具列會立即重新判斷上方、下方或覆蓋位置。
- 「可移動／已鎖定」按鈕切換位置鎖定；鍵盤 `L` 也可切換。
- 「透明−／透明+」以固定級距調整透明度。
- 工具列顯示目前縮放比例及透明度百分比。
- 「關閉」只關閉目前貼圖；原有右鍵與 `Esc` 關閉仍保留。
- 滾輪縮放以游標所在的螢幕座標為中心，並保持原始圖片比例。
- 工具列文字與深色背景的對比度須至少達 WCAG AA 4.5:1；透明度變更後也需檢查合成後的可讀性。

### PoC-C.2.1 緊湊圖示工具列

- 工具列使用 340 × 40 px 的淺灰色浮動列，不再延伸成與貼圖等寬的深色文字列。
- 外框使用 2 px 藍色提示線；按鈕以深色符號呈現，並以細灰線分組。
- 操作順序為：移動／鎖定、透明度降低、透明度提高、縮放與透明度狀態、關閉。
- 一般狀態只保留縮放及透明度數值，其他功能使用簡潔符號，接近圖示型編輯工具列。
- 前景 `#242A31` 與背景 `#F5F6F7` 必須通過 WCAG AA 4.5:1 對比檢查。

### PoC-C.2.2 原生向量圖示

- 操作按鈕不再以 `↔`、`α−`、`α+`、`×` 等字型字符冒充圖示。
- 移動使用四方向箭頭；鎖定使用鎖頭；透明度使用水滴搭配減號／加號；縮放使用放大鏡；關閉使用交叉線。
- 圖示由 GDI 2 px 向量線條繪製，不依賴使用者電腦的 emoji、符號字型或字型回退結果。
- 鎖定狀態會將移動圖示切換成鎖頭，讓狀態不只依靠顏色表達。

## 驗收矩陣

| 編號 | 測試 | 預期結果 |
|---|---|---|
| C1 | 建立貼圖 | 圖片以原始尺寸顯示在擷取時的全域座標，並保持置頂 |
| C2 | 拖曳貼圖 | 視窗平順移動，圖片內容不位移或閃爍 |
| C3 | 滾輪縮放 | 保持圖片比例，尺寸限制在 80 × 60 至 2400 × 1600 |
| C4 | Ctrl + 滾輪 | 透明度在約 19% 至 100% 間調整 |
| C5 | 多張貼圖 | 每張貼圖可獨立移動、縮放、調整透明度與關閉 |
| C6 | Esc／右鍵 | 用戶區或拖曳區的右鍵訊息都只關閉目前貼圖 |
| C7 | 邊界提示 | 圖片外緣顯示 2 px 薄荷綠細框 |
| C8 | 位置鎖定 | 鎖定後圖片區不能拖動，外部工具列仍可解鎖 |
| C9 | 游標中心縮放 | 滾輪縮放時游標指向的圖片位置保持在相同螢幕位置附近 |
| C10 | 數值提示 | 工具列的縮放與透明度百分比隨操作更新 |
| C11 | 工具列定位 | 依可用空間自動選擇貼圖上方、下方或圖片頂端覆蓋 |
| C12 | 圖示工具列 | 淺色背景、深色符號、細分隔線與藍色外框清晰且沒有文字黑底 |
| C13 | 向量圖示 | 所有操作顯示為一致線寬的功能圖示，且鎖定後移動圖示會切換為鎖頭 |

## 第一版限制

- 尚未提供滑鼠穿透；需搭配可復原穿透狀態的貼圖管理器或全域快捷鍵。
- 透明度為整個貼圖視窗的全域透明度。
- 貼圖在程式結束後不會自動復原。
# Direct2D 向量圖示驗收

- 工具列的移動、鎖定、透明度、縮放與關閉圖示應由 Direct2D 即時繪製，並啟用 per-primitive anti-aliasing。
- 分別在 Windows 縮放 100%、125%、150% 與 200% 觀察，圖示斜線與圓弧不應出現明顯鋸齒。
- 圖示保持 `#242A31` 與工具列 `#F5F6F7` 的 WCAG AA 4.5:1 以上對比。
- 確認各按鈕點擊範圍、工具列自動上下定位與貼圖操作均未改變。

## PoC-C.3 可復原滑鼠穿透

- 按工具列滑鼠指標圖示或鍵盤 `P`，切換當前貼圖的滑鼠穿透。
- 穿透開啟後，貼圖下方的視窗可接收滑鼠操作，但獨立工具列仍可點擊。
- 工具列顯示「穿透中」文字與交叉指標圖示，狀態不只依賴顏色。
- 再按一次圖示或 `P` 後，貼圖恢復拖曳、滾輪縮放、Ctrl+滾輪透明度與右鍵關閉。
