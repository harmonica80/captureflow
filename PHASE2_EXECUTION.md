# CaptureFlow 第 2 階段執行計畫

> 目標：完成不破壞底圖、可保存並重新編輯的物件式標註體驗。

## 2.1 非破壞式標註核心

- 截圖底圖與標註 SVG 圖層分離，編輯操作不修改原始 PNG。
- 第一批物件提供矩形與實心漸寬箭頭，座標以原始圖片像素保存，不受畫面縮放影響。
- 提供復原、重做及清除全部，選取工具同時使用文字與狀態樣式表示。
- `.captureflow.json` 專案格式包含 schema version、底圖路徑、畫布尺寸與物件陣列。
- 專案來源限制為 CaptureFlow 應用程式資料目錄內的截圖，單一專案最多 10,000 個物件。

### 2.2 專案重開與物件操作

- 可重新載入目前截圖最近儲存的 `.captureflow.json` 專案。
- 選取矩形或箭頭後，可拖曳移動並以控制點縮放或調整端點。
- 移動與縮放納入復原／重做；底圖與物件座標仍維持分離。
- 選取狀態同時使用外框、控制點與文字提示，不只依賴顏色。
- 控制點採白底藍框；選取物件可用工具列或 Delete／Backspace 刪除。
- 箭頭中央控制點可調整二次貝茲曲線；矩形內側控制點可調整圓角半徑。
- 框選完成後可用滑鼠滾輪調整截圖圓角，輸出 PNG 的角落會保留透明度。
- 框選範圍外顯示圓角圖示，可點擊快速開關圓角；滾輪仍可細調半徑。
- 標註物件在 hover 時顯示物件框，點擊後切換為原工具，滾輪調整線寬。
- 編輯器支援 Ctrl+Z 復原，以及 Ctrl+Shift+Z／Ctrl+Y 重做。
- Hover 已可直接選取物件，因此移除獨立選取工具；選取後只顯示控制點，不再繪製黑色外框。
- 完成框選、重複上次範圍或擷取螢幕後，自動開啟物件式標註器並捲動至編輯區。
- 框選完成後在範圍旁顯示原生浮動工具列，提供矩形、箭頭、復原、完成及取消；標註直接合成至 PNG。
- 原生覆蓋層使用雙緩衝並抑制背景擦除；工具列改用 PixPin 類型的淺色向量圖示，箭頭與編輯器共用實心漸寬幾何。
- 原生標註支援 hover 命中、控制點、拖曳移動與滾輪調整 1–32 px 線寬；工具列加入 PixPin 式點陣握把與分組留白。

## 後續增量

1. 畫筆、螢光筆、文字與自動編號。
2. 馬賽克、模糊與實色遮蔽。
3. 壓平匯出、物件橡皮擦與自動恢復。
### 原生框選物件互動補強

- 截取框邊線顯示四向移動游標，八個控制點依方向顯示雙向縮放游標。
- 原生矩形與箭頭控制點可直接拖曳調整；箭頭新增中央曲率節點並以向量多邊形繪製。
- 浮動標註工具列新增顏色色票按鈕，可循環選擇紅、藍、綠、橙、紫與深灰，並套用到目前選取物件。
- 游標狀態除了形狀也保留控制點提示，不只以顏色傳達互動狀態。
