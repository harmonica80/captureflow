# CaptureFlow 第 2 階段執行計畫

> 目標：完成不破壞底圖、可保存並重新編輯的物件式標註體驗。

## 2.1 非破壞式標註核心

- 截圖底圖與標註 SVG 圖層分離，編輯操作不修改原始 PNG。
- 第一批物件提供矩形與實心漸寬箭頭，座標以原始圖片像素保存，不受畫面縮放影響。
- 提供復原、重做及清除全部，選取工具同時使用文字與狀態樣式表示。
- `.captureflow.json` 專案格式包含 schema version、底圖路徑、畫布尺寸與物件陣列。
- 專案來源限制為 CaptureFlow 應用程式資料目錄內的截圖，單一專案最多 10,000 個物件。

### 2.2 專案重開與物件操作

- 可重新載入目前截圖最近儲存的 `.captureflow.json` 專案。
- 選取矩形或箭頭後，可拖曳移動並以控制點縮放或調整端點。
- 移動與縮放納入復原／重做；底圖與物件座標仍維持分離。
- 選取狀態同時使用外框、控制點與文字提示，不只依賴顏色。

## 後續增量

1. 畫筆、螢光筆、文字與自動編號。
2. 馬賽克、模糊與實色遮蔽。
3. 壓平匯出、物件橡皮擦與自動恢復。
