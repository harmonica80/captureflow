# PoC-B：原生跨螢幕選取層測試

## 目的

驗證 CaptureFlow 能在 Windows virtual desktop 上顯示凍結畫面、跨螢幕拖曳選取，並將全域座標正確轉換成裁切 PNG。

## 操作方式

1. 執行 `CaptureFlow-portable.exe`。
2. 按下「開始框選螢幕範圍」。
3. 在凍結畫面上按住滑鼠左鍵拖曳。
4. 放開後檢查綠色選取框。
5. 按 `Enter` 確認並輸出；按 `Esc` 取消。

輸出位於應用程式資料目錄的 `poc-b` 資料夾，包含：

- `selection-時間戳.png`
- `selection-時間戳.json`

## 第一版驗收矩陣

| 案例 | 操作 | 驗收 |
|---|---|---|
| B1 | 在主螢幕內由左上往右下框選 | PNG 尺寸及內容正確 |
| B2 | 在主螢幕內反方向框選 | 自動正規化為正寬高 |
| B3 | 在第二螢幕框選 | JSON X 座標包含第二螢幕偏移 |
| B4 | 從主螢幕跨到第二螢幕 | PNG 跨越交界且無缺口 |
| B5 | 拖曳後按 Esc | 不輸出檔案並返回主視窗 |
| B6 | 拖曳後按 Enter | 輸出 PNG/JSON 並顯示路徑 |
| B7 | 未框選直接按 Enter | 不關閉選取層、不輸出空圖片 |
| B8 | 125% 與 100% 混合 DPI | 選框與輸出像素沒有縮放偏移 |

## 人工檢查

- 開啟選取層時畫面應凍結，不因背景視窗更新而改變。
- 覆蓋層應完整涵蓋兩部螢幕。
- 游標應為十字形。
- 拖曳過程選框跟隨滑鼠，且不留下殘影。
- PNG 邊界與使用者看到的選框一致。
- PNG 顏色正常，沒有紅藍互換。
- `Esc` 後可以再次啟動選取模式。

## PoC-B.2 框選互動強化

本階段在既有跨螢幕框選基礎上加入：

- 全桌面暗色遮罩，選取區域維持原始亮度。
- 框選期間與框選完成後即時顯示像素尺寸。
- 選取框四角與四邊共八個調整控制點。
- 放開滑鼠後可拖曳任一控制點調整範圍，再按 `Enter` 輸出。
- 控制點調整限制在 virtual desktop 邊界內，最小尺寸為 2 × 2。

建議驗收：先建立一個跨螢幕選取框，依序拖曳四角與四邊控制點，確認暗色遮罩、尺寸文字與輸出的 PNG/JSON 尺寸同步更新。

### 固定快照回歸測試

- 拖曳建立選取框時，框內桌面內容必須固定在原本的螢幕座標，不得隨框線移動或縮放。
- 拖曳八個控制點調整選取框時，只能改變亮區與遮罩邊界；框內視窗、文字及圖示不得位移或變形。
- 調整完成後輸出的 PNG 應與選取框內當時看到的固定快照一致。

## PoC-B.3 鍵盤像素微調

- 框選完成後，按方向鍵將整個選取框向對應方向移動 1 像素。
- 按住 `Shift` 再按方向鍵，每次快速移動 10 像素。
- 選取框抵達 virtual desktop 邊界時停止，不縮小且不超出桌面。
- 鍵盤移動期間，尺寸保持不變，固定快照、暗色遮罩、控制點與尺寸文字同步更新。

建議驗收：建立跨螢幕選取框後，分別以方向鍵和 `Shift + 方向鍵` 移動至四個桌面邊界，再按 `Enter`，確認 PNG/JSON 座標變化與畫面一致。

## PoC-B.4 智慧視窗偵測

- 尚未開始框選時，移動游標會辨識該位置下方最上層、可見且未最小化的 Windows 頂層視窗。
- 偵測到的視窗以亮區及綠色邊框預覽，但不顯示八方向控制點。
- 單擊採用預覽的整個視窗範圍；按 `Enter` 後輸出 PNG/JSON。
- 從預覽視窗內按下並拖曳超過 4 像素，會切換為自由框選，不強制選取整個視窗。
- 採用視窗範圍後，仍可使用八方向控制點、方向鍵與 `Shift + 方向鍵` 調整。
- 超出 virtual desktop 的視窗邊界會被裁切在可擷取範圍內。

建議驗收：依序將游標移到瀏覽器、檔案總管、CaptureFlow 與跨螢幕視窗，確認預覽邊界；分別測試單擊選取和按住拖曳自由框選。

### 單幀合成回歸測試

- 選取框建立後持續移動滑鼠，框內畫面與暗色區域不得閃爍。
- 連續拖曳四邊及四角控制點時，畫面只顯示已完成合成的單一幀，不得交替出現全暗與局部亮區。
- 智慧視窗預覽在不同視窗間切換時，不得出現上一個視窗的亮區殘影。

## 目前已知限制

- 尚未加入視窗內部控制項層級的智慧偵測。
- 目前需按 `Enter` 確認，不會在放開滑鼠時直接輸出。
- 原生層使用 GDI 顯示 PoC-A 快照；錄影後端仍另行選型。
