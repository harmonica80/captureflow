# CaptureFlow 下一批開發待辦（2026-08-04）

本文件只記錄需求與建議實作方向；本次不修改程式功能。待累積更多修正項目後一併開發及驗收。

## 1. 從左側選單開啟 JSON 專案

- 將「開啟 JSON 專案」從編輯器右上方動作列移至左側選單。
- 即使尚未擷取或載入圖片，也必須能直接開啟既有 CaptureFlow JSON 專案。
- 開啟自包含 schema 2 專案時，還原內嵌來源圖片、畫布尺寸與全部可編輯物件。
- 編輯器動作列可保留「儲存 JSON 專案」，但不再重複顯示開啟按鈕。

## 2. 自動保留最近 20 次截圖與編修專案

- 每次完成截圖後建立一筆歷史紀錄。
- 編修內容變更後，為該筆紀錄保存可重新編輯的 JSON 專案快照。
- 歷史數量上限為 20 筆；新增第 21 筆時移除最舊一筆。
- 移除時需同步清理對應圖片及自動 JSON，避免應用程式資料目錄持續膨脹。
- 使用者手動另存的 JSON 不屬於自動清理範圍。
- 建議採用索引檔管理紀錄，而不是只依檔名或檔案時間排序。

## 3. 「重複上次範圍」改為「歷史截圖範圍」

- 按鈕名稱改成「歷史截圖範圍」。
- 點擊後顯示最近 20 次截圖紀錄，而不是立即重複最後一次擷取。
- 每筆至少顯示縮圖、建立時間、擷取尺寸及螢幕／全域座標摘要。
- 點擊歷史項目後載入該筆原始圖片與自動保存的可編輯 JSON。
- 若仍需要「再次擷取相同範圍」，可放在各歷史項目的次要動作中。

## 4. 頁尾開發者與連結

- 頁面下方新增「述文老師開發」。
- 新增部落格連結：`https://harmonica80.blogspot.com/`。
- 新增 GitHub 專案連結；開發階段先使用虛擬網址，例如：
  `https://github.com/harmonica80/captureflow-placeholder`
- 外部連結須有清楚名稱、鍵盤焦點樣式及 WCAG AA 4.5:1 對比。
- 正式發布前將虛擬 GitHub 網址替換成公開專案網址。

## 5. 標題列版本與 GitHub 新版檢查

- 應用程式標題列顯示軟體名稱與版本，例如 `CaptureFlow 0.1.0`。
- 版本號以 Tauri／Cargo 套件版本為單一來源，避免前端另外維護造成不一致。
- 程式啟動後背景查詢 GitHub Releases 最新正式版本。
- 僅提示更新，不自動下載或執行安裝檔。
- 更新提示需提供版本號、發布頁連結與「稍後提醒／忽略此版本」。
- 網路中斷、GitHub API 限流或尚無 Release 時，不得阻止程式啟動，也不應持續顯示錯誤。
- 建議加入檢查節流，例如每 24 小時最多自動查詢一次；另提供手動「檢查更新」。
- 正式 GitHub 儲存庫及 Releases 上線前，先使用可替換的設定常數，不連接虛擬 API。

## 後續驗收重點

- 無圖片狀態可直接開啟 JSON。
- 關閉並重啟程式後，20 筆歷史仍存在。
- 第 21 筆建立後，最舊的自動圖片與 JSON 都被清除，手動專案不受影響。
- 載入歷史項目後，全部物件仍可移動、調整及復原。
- 無網路環境可正常啟動，不出現阻塞式更新錯誤。

